@model Proyecto_LibreriaRincondelQuijote.Models.HistorialCompra

@{
    ViewBag.Title = "Delete";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>

</head>
<body class="bg-light">
    <div class="container">
        <h3 class="text-danger">Are you sure you want to delete this?</h3>
        <h4 class="text-primary">HistorialCompra</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>@Html.DisplayNameFor(model => model.Producto.Nombre)</dt>
            <dd>@Html.DisplayFor(model => model.Producto.Nombre)</dd>

            <dt>@Html.DisplayNameFor(model => model.UserId)</dt>
            <dd>@Html.DisplayFor(model => model.UserId)</dd>

            <dt>@Html.DisplayNameFor(model => model.Precio)</dt>
            <dd>@Html.DisplayFor(model => model.Precio)</dd>

            <dt>@Html.DisplayNameFor(model => model.FechaCompra)</dt>
            <dd>@Html.DisplayFor(model => model.FechaCompra)</dd>

            <dt>@Html.DisplayNameFor(model => model.Cantidad)</dt>
            <dd>@Html.DisplayFor(model => model.Cantidad)</dd>
        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-actions">
                <input type="submit" value="Delete" class="btn btn-danger" /> |
                @Html.ActionLink("Back to List", "Index", new { @class = "btn btn-secondary" })
            </div>
        }
    </div>
</body>
</html>
